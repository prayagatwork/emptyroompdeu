<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reservations</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f2f2f2;
    }
    .container {
      max-width: 600px;
      margin: 50px auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .form-group {
      margin-bottom: 20px;
    }
    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }
    input[type="text"], input[type="email"], input[type="date"], select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      box-sizing: border-box;
    }
    input[type="submit"], .button {
      background-color: #4caf50;
      color: #fff;
      border: none;
      padding: 10px 20px;
      cursor: pointer;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }
    input[type="submit"]:hover, .button:hover {
      background-color: #45a049;
    }
    .form-group .error {
      color: red;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Reserve Room</h2>
    <form id="myForm" action="/rsvp" method="POST" onsubmit="return validateEmail()">
      <div class="form-group">
        <label for="name">User Name:</label>
        <input type="text" id="name" name="Name" required>
      </div>
      <div class="form-group">
        <label for="BlockId">Block:</label>
        <input type="text" id="BlockId" name="BlockId" value="<%= block %>" readonly>
    </div>
      <div class="form-group">
        <label for="RoomNo">Room Number:</label>
        <input type="text" id="roll" value="<%= roomNumber %>" name="RoomNo" readonly>
      </div>
      <div class="form-group">
        <label for="Date">Date:</label>
        <input type="date" id="work" name="Date" required>
      </div>
      <div class="form-group">
        <label for="Details">Organization & Reason:</label>
        <input type="text" id="work" name="Details" required>
      </div>
      <div class="form-group">
        <label for="ReservationType">Reservation Type:</label>
        <select id="ReservationType" name="ReservationType" required>
            <option value="temporary">Temporary</option>
            <option value="permanent">Permanent</option>
        </select>
      </div>
      <div class="form-group">
        <label for="Email">Email ID:</label>
        <input type="email" id="email" name="Email" required>
        <span id="emailError" class="error"></span> <!-- Display email validation error message here -->
      </div>
      <button type="submit" class="button">Send Request</button>
    </form>
  </div>

  <script>
    function validateEmail() {
      var email = document.getElementById("email").value;
      var regex = /^[a-zA-Z0-9._%+-]+@(sot.pdpu.ac.in)$/; // Regular expression for your organization's email domain

      if (!regex.test(email)) {
        document.getElementById("emailError").textContent = "Invalid email / Enter PDEU Email-ID";
        return false; // Prevent form submission
      }
      return true; // Allow form submission
    }
  </script>
</body>
</html>
